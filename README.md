# 🏇 СОЛЛАЛЬ 2026. УРАЛЬСКИЕ СКАЧКИ

Интерактивная игра для празднования корейского нового года с управлением через встряхивание телефона.

## 📋 Требования

- Node.js 14+ (рекомендуется 18+)
- npm или yarn
- Современный браузер (Chrome, Firefox, Safari, Edge)

## 🚀 Установка

### 1. Установите зависимости

```bash
npm install
```

### 2. Запустите сервер

```bash
npm start
```

Сервер запустится на порту 3000 (по умолчанию).

### 3. Откройте в браузере

Перейдите на `http://localhost:3000` или `http://ваш-ip:3000`

## 📱 Использование

### Для главного экрана (проектор)

1. Откройте приложение на компьютере с проектором
2. Экран автоматически покажет стартовую страницу с QR-кодом
3. Когда зарегистрируется нужное количество команд, нажмите **СТАРТ**
4. Наблюдайте за гонкой на большом экране!

### Для участников (телефоны)

1. Отсканируйте QR-код со стартового экрана
2. Выберите свой стол/команду
3. Первый игрок команды придумывает название
4. Остальные игроки присоединяются к команде
5. После старта гонки - трясите телефон!

**Альтернатива встряхиванию:** Если встряхивание не работает, можно зажать палец на экране.

### Для администратора

1. Откройте `http://localhost:3000` в браузере
2. Нажмите кнопку **"Админ"** справа снизу
3. Настройте параметры:
   - Количество столов/команд (2-12)
   - Максимум игроков от команды (1-20)
   - Минимум команд для старта (2-12)
   - Коэффициент скорости (0.1-5)
4. Изменения применяются автоматически

## ⚙️ Настройка для локальной сети

### Вариант 1: Использование локального IP

1. Узнайте IP-адрес вашего компьютера:
   - Windows: `ipconfig` в командной строке
   - Mac/Linux: `ifconfig` в терминале
   
2. Например, если IP: `192.168.1.100`, то:
   - Главный экран: `http://192.168.1.100:3000`
   - QR-код будет содержать: `http://192.168.1.100:3000?view=player`

### Вариант 2: Использование другого порта

Если порт 3000 занят, установите переменную окружения:

```bash
# Windows
set PORT=8080
npm start

# Mac/Linux
PORT=8080 npm start
```

## 🌐 Развертывание на сервере

### Для продакшена

1. Установите PM2 для управления процессом:

```bash
npm install -g pm2
```

2. Запустите приложение:

```bash
pm2 start server.js --name "horse-race"
```

3. Настройте автозапуск:

```bash
pm2 startup
pm2 save
```

### Использование с Nginx (опционально)

Если вы хотите использовать доменное имя или HTTPS:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 🎮 Особенности игры

### Механика

- Каждый игрок трясет свой телефон
- Интенсивность встряхивания суммируется в команде
- Чем больше игроков и чем сильнее трясут - тем быстрее лошадь
- Первая команда, достигшая финиша, побеждает

### Коэффициент скорости

- **0.1** - очень медленно (финиш за ~5 минут)
- **1.0** - стандарт (финиш за ~30 секунд при активном участии)
- **2.0** - быстро (финиш за ~15 секунд)
- **5.0** - очень быстро (финиш за ~6 секунд)

## 🔧 Разработка

Для разработки с автоперезагрузкой:

```bash
npm install --save-dev nodemon
npm run dev
```

## 📂 Структура проекта

```
.
├── server.js           # Серверная часть с WebSocket
├── package.json        # Зависимости
├── public/
│   └── index.html      # Клиентская часть (главный экран, игрок, админ)
└── README.md           # Эта инструкция
```

## 🐛 Решение проблем

### Проблема: Устройства не подключаются

**Решение:**
1. Убедитесь, что все устройства в одной Wi-Fi сети
2. Проверьте, что файрвол не блокирует порт 3000
3. Используйте IP-адрес вместо localhost

### Проблема: Встряхивание не работает на iPhone

**Решение:**
1. Убедитесь, что сайт открыт через HTTPS (для доступа к сенсорам)
2. Используйте альтернативный метод - зажатие пальца на экране
3. В iOS 13+ может потребоваться разрешение на использование датчиков движения

### Проблема: Игра тормозит

**Решение:**
1. Уменьшите количество столов/команд
2. Ограничьте количество игроков на команду
3. Используйте более мощный компьютер для сервера

## 📞 Поддержка

Если возникли вопросы или проблемы, проверьте:
1. Консоль браузера (F12) на наличие ошибок
2. Логи сервера в терминале
3. Статус подключения (индикатор в правом верхнем углу)

## 🎉 Приятной игры!

Желаем веселого празднования Соллаля 2026! 🐍✨
